# LangChain Agent (Starter)

Simple, modular LangChain agent project using OpenAI LLM + tools, memory and middleware.

## Quickstart

1. Create and activate a virtual environment:
   ```bash
   python -m venv .venv
   # Windows
   .\.venv\Scripts\activate
   # macOS / Linux
   source .venv/bin/activate
